(ns migrations.core
  (:gen-class))

;; region Main program (not used)

(defn -main
  "This does not do much, the project is mainly focused on testing."
  [& args]
  (println "Don't start me, run `lein eftest`, instead."))

;; endregion
